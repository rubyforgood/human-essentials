<tr>
  <td><%= donation_row.source_view %></td>
  <td><%= donation_row.created_at.strftime("%F") %>
  <td><%= donation_row.donation_site.try(:name) %></td>
  <td><%= donation_row.storage_location.name %></td>
  <td><%= donation_row.line_items.total %></td>
  <td><%= donation_row.line_items.size %></td>
  <td class="text-right">
   <%= link_to "View", donation_path(donation_row), class: "btn btn-primary btn-xs" %>
  </td>
</tr>
