<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : default_title_content %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>

    <%= csrf_meta_tags %>
  </head>

  <body id="<%= params[:controller] || "" %>" class="<%= params[:action] || "" %>">
    <% if params[:controller].include?("admins") %>
    <%= render partial: "layouts/admin_navbar" %>
    <% else %>
    <%= render partial: "layouts/navbar" %>
    <% end %>
    <%= display_flash_messages %>

    <div class="row">
      <% if content_for?(:sidebar) %>
        <div class="primary medium-4 large-3 columns"><%= yield :sidebar %></div>
        <div class="primary medium-8 large-9 columns"><%= yield %></div>
      <% else %>
        <div class="primary medium-12 large-12 columns"><%= yield %></div>
      <% end %>
    </div>

  </body>
</html>
