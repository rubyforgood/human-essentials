<% content_for :title, "Edit #{@storage_location.name} - Storage Locations - Inventory - #{current_organization.name}" %>

<div class="px-4 py-6 sm:px-6 lg:px-8">
  <%= render "shared/essentials/page_header",
        title: "Edit #{@storage_location.name}",
        back: {path: storage_locations_path, label: "Back to storage locations"} %>
  <%= render partial: "form", object: @storage_location %>
</div>
