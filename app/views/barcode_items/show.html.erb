<% content_for :title, "#{current_organization.name} - Inventory - Barcode Items - #{@barcode_item.item.name}" %>


<h1><%= @barcode_item.item.name %></h1>

<dl>
    <dt>Quantity</dt>
	<dd><%= @barcode_item.quantity %></dd>

	<dt>Barcode</dt>
	<dd><%= @barcode_item.value %></dd>
</dl>