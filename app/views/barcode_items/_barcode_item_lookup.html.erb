<%# Increment a counter on how many we've seen so that we can create a unique ID -- really only necessary for test hooks %>
<input type="text" id="<%= "_barcode-lookup-#{index}" %>" class="__barcode_item_lookup form-control" value="" placeholder="Barcode Entry" autocomplete="on">
