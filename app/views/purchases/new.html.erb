<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "New - Purchases - #{current_organization.name}" %>
        <h1>
          Start a new purchase
          <small>for <%= current_organization.name %></small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(dashboard_path) do %>
              <i class="fa fa-dashboard"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><%= link_to "Purchases", purchases_path %></li>
          <li class="breadcrumb-item"><a href="#">New Purchase</a></li>
        </ol>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <!-- left column -->
      <div class="col-md-12">
        <!-- jquery validation -->
        <div class="card card-primary">
          <!-- /.card-header -->
          <!-- form start -->
          <div class="card-body">
            <%= render partial: "purchase_form", object: @purchase %>
            <%= link_to "Add vendor", new_vendor_path, {:remote => true, "data-bs-target" => "#modal-window", id: "new_vendor", "style" => "display: none;"} %>
          </div><!-- /.box -->
        </div>
      </div>
    </div>
  </div>
</section>
<%= render partial: "barcode_items/barcode_modal" %>
