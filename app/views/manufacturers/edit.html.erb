<section class="content-header">
<% content_for :title, "Edit - Manufacturers - #{@manufacturer.name} - #{current_organization.name}" %>
<h1>
  Editing Manufacturer
  <small>for <%= current_organization.name %></small>
</h1>
<ol class="breadcrumb">
<li><%= link_to(dashboard_path) do %>
    <i class="fa fa-dashboard"></i> Home
  <% end %>
  </li>
  <li><%= link_to "Manufacturers", (manufacturers_path) %></li>
  <li class="active">Editing <%= @manufacturer.name %></li>
</ol>
</section>

<!-- Main content -->
<section class="content">

<!-- Default box -->
<div class="box">
  <div class="box-header with-border">
    <h3 class="box-title">Update record for <%= @manufacturer.name %></h3>
  </div>
  <div class="box-body">
		<%= render partial: "form", object: @manufacturer %>
  </div>
</div>
<!-- /.box -->

</section>
