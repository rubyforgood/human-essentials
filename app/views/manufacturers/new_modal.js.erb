// Injects the dialog and opens it. showModal() supplies the backdrop, focus trap and
// Escape handling that the Bootstrap modal used to approximate in JS.
document.getElementById("modal_new").innerHTML = "<%= j(render "manufacturers/new_modal") %>";
document.querySelector("#modal_new dialog").showModal();
