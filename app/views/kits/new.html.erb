<% content_for :title, "New Kit - Inventory - #{current_organization.name}" %>

<div class="px-4 py-6 sm:px-6 lg:px-8">
  <%= render "shared/essentials/page_header",
        title: "New kit",
        subtitle: "A kit bundles several items so they can be distributed as one unit.",
        back: {path: kits_path, label: "Back to kits"} %>
  <%= render "form" %>
</div>

<%= render partial: "barcode_items/barcode_modal" %>
