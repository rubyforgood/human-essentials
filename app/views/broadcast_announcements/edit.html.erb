<section class="mb-6">
  <div class="px-4 py-6 sm:px-6 lg:px-8">
    <div class="mb-2">
      <div class="min-w-0">
        <% content_for :title, "Send broadcast announcement" %>
        <h1 class="text-2xl font-bold tracking-tight text-slate-900">Edit Broadcast Announcement</h1>
<p class="mt-1 text-sm text-slate-600">for <%= current_organization.name %></p>
      </div>
      <div class="min-w-0">
      </div>
    </div>
  </div>
</section>

<%= render "form", broadcast_announcement: @broadcast_announcement %>
