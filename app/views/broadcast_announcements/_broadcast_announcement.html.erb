<tr id="<%= dom_id broadcast_announcement %>">
  <td><%= broadcast_announcement.message %></td>
  <td><%= link_to  broadcast_announcement.link, broadcast_announcement.link %></td>
  <td><%= broadcast_announcement.user&.name || "N/A" %></td>
  <td class="date text-left"><%= broadcast_announcement.expiry %>
    <% if broadcast_announcement.expired? %>
      <%= essentials_status_pill("Expired", tone: :danger, icon: "bi-clock-history") %>
    <% end %>
  </td>
  <td class="text-right">
    <div class="flex justify-end gap-1.5">
      <%= essentials_link_button "Edit", edit_broadcast_announcement_path(broadcast_announcement), variant: :ghost, size: :sm, icon: "bi-pencil" %>
      <%= essentials_action_button "Delete", broadcast_announcement, method: :delete,
            variant: :ghost_danger, size: :sm, icon: "bi-trash",

            confirm: "Are you sure?" %>
    </div>
  </td>
</tr>
