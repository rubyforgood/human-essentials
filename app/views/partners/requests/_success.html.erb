<%#
  The request went through. Same story as _error: this was the 2021 design and is now the
  callout component.
%>
<%= render "shared/essentials/callout", tone: :success, wrapper_class: "mb-6",
      title: "Request sent" do %>
  <p class="mt-1">
    <%= current_partner.organization.name %> has your request. A confirmation email will follow
    in a few minutes.
  </p>
<% end %>
