<% content_for :title, "Edit #{@partner.name} - Agencies - #{current_organization.name}" %>

<div class="px-4 py-6 sm:px-6 lg:px-8">
  <%= render "shared/essentials/page_header",
        title: "Edit #{@partner.name}",
        back: {path: partner_path(@partner), label: "Back to #{@partner.name}"} %>
  <%= render partial: "form", object: @partner, locals: {submit_btn_options: {text: "Update partner"}} %>
</div>
