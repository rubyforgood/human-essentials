<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "Children - #{current_partner.name}" %>
        <h1><i class="fa fa-user"></i>&nbsp;&nbsp;
          Authorized Family Member Details
          <small>for <%= current_partner.name %></small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(partner_user_root_path) do %>
              <i class="fa fa-home fa-lg"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><a href="<%= partners_family_path(@authorized_family_member.family) %>">
            The <%= "#{@authorized_family_member.family.guardian_last_name}" %> Family</a></li>
          <li class="breadcrumb-item"><a href="#">View Authorized Family Member</a></li>
        </ol>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</section>

<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-12">
        <div class="card mb-12">
          <h5 class="card-header bg-primary text-white">Authorized Family Member Information</h5>
          <div class="card-body">
            <dl>
              <dt>First Name:</dt>
              <dd><%= @authorized_family_member.first_name %></dd>

              <dt>Last Name:</dt>
              <dd><%= @authorized_family_member.last_name %></dd>

              <dt>Date of Birth:</dt>
              <dd><%= @authorized_family_member.date_of_birth %></dd>

              <dt>Gender:</dt>
              <dd><%= @authorized_family_member.gender %></dd>

              <dt>Comments:</dt>
              <dd><%= @authorized_family_member.comments %></dd>
            </dl>
          </div>
          <div class="card-footer">
            <%= link_to 'Edit Authorized Family Member Details', edit_partners_authorized_family_member_path(@authorized_family_member), class: "btn btn-success" %>
            <%= link_to 'Show Member Family', partners_family_path(@authorized_family_member.family), class: "btn btn-info pull-right" %>
          </div>
        </div>
      </div>
    </div>
  </div>

</section>
