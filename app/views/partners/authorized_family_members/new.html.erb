<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "Families - #{current_partner.name}" %>
        <h1><i class="fa fa-user"></i>&nbsp;&nbsp;
          New Authorized Family Member
          <small>for <%= current_partner.name %></small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(partner_user_root_path) do %>
              <i class="fa fa-home fa-lg"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><a href="<%= partners_family_path(@authorized_family_member.family) %>">
            The <%= "#{@authorized_family_member.family.guardian_last_name}" %> Family</a></li>
          <li class="breadcrumb-item"><a href="#">New Authorized Family Member</a></li>
        </ol>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</section>

<%= render 'form', authorized_family_member: @authorized_family_member %>
