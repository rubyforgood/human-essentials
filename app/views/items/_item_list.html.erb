<div id="items-list">
  <div class="table-scroll" tabindex="0" role="region" aria-label="Table, scrollable">
    <table id="items-table" class="data-table">
      <caption>Every item this organization stocks</caption>
      <thead>
        <tr>
          <th scope="col">Category</th>
          <th scope="col">Name</th>
          <th scope="col">NDBN reporting category</th>
          <th scope="col">Additional info</th>
          <th scope="col" class="quantity">Quantity per individual</th>
          <th scope="col" class="numeric">Fair market value (per item)</th>
          <% if Flipper.enabled?(:enable_packs) && current_organization.request_units.any? %>
            <th scope="col" class="notes">Custom request units</th>
          <% end %>
          <th scope="col" class="cell-actions">Actions</th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "items/item_row", collection: items, as: :item_row, locals: {inventory: inventory, kits: kits} %>
      </tbody>
    </table>
  </div>
  <%#
    The controller has always computed a paginated collection; this panel rendered the full one
    and never showed a pager, so 51 items at 121px a row made a 6,442px page. See design.md.

    The chrome is inline rather than the card's `footer:` slot. That was forced while the card
    wrapped five panels -- a card footer would have shown the item pager under the Kits tab too.
    Now that the tabs are pages the card wraps one table, so it *could* move to `footer:`.

    **Decided on 2026-09-04 not to.** It renders identically either way, so the move buys nothing a
    user or a reader can see, and `footer:` is not a rule the design system enforces -- other pages
    use both. A change with no observable effect is one nobody can review and nobody can regression
    test; the only argument for it was tidiness, and this comment serves that more cheaply. Revisit
    only if the card gains a footer for some other reason, at which point having two is the fault.
  %>
  <% if (pager = essentials_pagination_footer(items)) %>
    <div class="border-t border-slate-200 px-5 py-3"><%= pager %></div>
  <% end %>
</div>
