<tr>
  <td><%= item_row.name %></td>
  <td><%= item_row.category %></td>
  <td><%= item_row.barcode_count %></td>
  <td><%= link_to "View", item_path(item_row) %>    
      <%= link_to "Edit", edit_item_path(item_row) %> 
      <%= link_to "Delete", item_path(item_row), method: :delete %></td>
</tr>