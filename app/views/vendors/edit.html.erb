<section class="content-header">
<% content_for :title, "Edit - Vendors - #{@vendor.contact_name} - #{current_organization.name}" %>
<h1>
  Editing Vendor
  <small>for <%= current_organization.name %></small>
</h1>
<ol class="breadcrumb">
<li><%= link_to(dashboard_path) do %>
    <i class="fa fa-dashboard"></i> Home
  <% end %>
  </li>
  <li><%= link_to "Vendors", (vendors_path) %></li>
  <li class="active">Editing <%= @vendor.contact_name %></li>
</ol>
</section>

<!-- Main content -->
<section class="content">

<!-- Default box -->
<div class="box">
  <div class="box-header with-border">
    <h3 class="box-title">Update record for <%= @vendor.contact_name %></h3>
  </div>
  <div class="box-body">
		<%= render partial: "form", object: @vendor %>
  </div>
</div>
<!-- /.box -->

</section>
