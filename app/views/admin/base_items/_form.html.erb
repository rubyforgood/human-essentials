<%#
  Locals: form (a form builder).

  The fields were wrapped in `f.input ... do` blocks that pasted the input classes by hand,
  which is what the :essentials wrapper already does. Plain `f.input` gets the same result and
  keeps the error message.
%>
<%= form.input :name, label: "Name" %>
<%= form.input :partner_key, label: "Key, for partner requests" %>
