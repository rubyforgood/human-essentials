<section class="content-header">
<% content_for :title, "Organizations - New" %>
<h1>
New Organization
<small></small>
</h1>
<ol class="breadcrumb">
  <li><%= link_to(dashboard_path(organization_id: current_user.organization)) do %>
    <i class="fa fa-dashboard"></i> Home
  <% end %>
  </li>
  <li><%= link_to "Administration", new_admin_organization_path %></li>
  <li><a href="#">New Organization</a></li>
</ol>
</section>

<!-- Main content -->
<section class="content">

<!-- Default box -->
<div class="box">
<div class="box-header with-border">
  <h3 class="box-title">Add New Diaperbase Organization</h3>
</div>
<div class="box-body">
<%= simple_form_for @organization, url: admin_organizations_path do |f| %>

<div class="form-inputs">
  <%= f.input :name, required: true, autofocus: true %>
  <%= f.input :short_name, required: true, autofocus: true %>
  <%= f.input :url, as: :url, placeholder: "http://www.example.com" %>
  <%= f.input :email %>
  <%= f.input :street %>
  <%= f.input :city %>
  <%= f.input :state, collection: us_states, class: "form-control", placeholder: "state" %>
  <%= f.input :zipcode %>
  <%= f.input :logo %>
  <p class="help-block">Logo should be a 4:1 ratio of width / height (default image is 763 x 188 pixels).
    Only jpeg/png format is supported.
  </p>

    <%= f.button :submit, "Create Organization", class: "btn btn-primary pull-right" %>
</div>

</div>

<% end %>
</div>
</div>
<!-- /.box -->

</section>
