<% content_for :title, "Annual surveys - Reports - #{current_organization.name}" %>
<div class="px-4 py-6 sm:px-6 lg:px-8">
  <%# main's #4987 adds a range export across every available year. It goes in the page header's
      actions rather than floating above the card: design.md gives a page one place for its
      actions, and an export is one. Hidden when there are no years, because the range would be
      empty and the button would download a file with nothing in it. %>
  <%= render "shared/essentials/page_header",
        title: "Annual survey",
        subtitle: "The yearly figures, one page per year.",
        back: {path: reports_path, label: "Back to reports"},
        actions: capture {
          if @years.any?
            concat essentials_link_button(
              "Export all years",
              range_reports_annual_reports_path(year_start: @years.first, year_end: @years.last, format: :csv),
              variant: :secondary, icon: "bi-box-arrow-up")
          end
        } %>

  <div class="space-y-4">
  <%= render "shared/essentials/callout", tone: :info, role: "note" do %>
    Reports are available at the end of every year.
    <%= @actual_year %> becomes available on <%= available_date %>.

  <% end %>

  <%= render "shared/essentials/card", title: "NDBN annual reports", padded: false do %>
    <% if @years.any? %>
      <ul class="divide-y divide-slate-100">
        <% @years.each do |year| %>
          <li>
            <%= link_to year, reports_annual_report_path(year),
                  class: "block px-5 py-3 text-sm font-medium link-brand transition-colors hover:bg-slate-50 focus-visible:outline-2 focus-visible:outline-offset-[-2px] focus-visible:outline-brand-600" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <div class="px-5 py-4">
        <%= render "shared/essentials/empty_state", kind: :cold_start,
              title: "No years yet",
              body: "A year appears here once it has ended." %>
      </div>
    <% end %>
  <% end %>
</div>
</div>
