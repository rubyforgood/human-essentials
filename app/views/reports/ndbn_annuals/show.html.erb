<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "Audits - Inventory - #{current_organization.name}" %>
        <h1>
          <%= "#{params[:year]} NDBN Annual Report" %>
          <small>for <%= current_organization.name %></small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(dashboard_path) do %>
              <i class="fa fa-dashboard"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><%= link_to "All Reports", reports_ndbn_annuals_path %></li>
          <li class="breadcrumb-item"><a href="#">Reports - <%= params[:year] %></a></li>
        </ol>
      </div>
    </div>
  </div>
</section>
<!-- Main content -->
<section class="content">
  <div class="card-footer">
    <div class="card-body">
      <span class="float-right">
        <%=
            download_button_to(
              reports_ndbn_annual_path(@year, format: :csv),
              text: "Export NDBN Annual Report"
            )
        %>
      </span>
    </div>
  </div>
</section>
<% @reports.each do |report| %>
  <section class="content">
    <h1 class='text-3xl text-center'><%= report.name %></h1><br />
    <div class="card">
      <div class="card-body p-0">
        <table class="table">
          <tbody>
          <% report.each_entry do |name, value| %>
            <tr>
              <th><%= name %></th>
              <td><%= value %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </section>
  <br/>
<% end %>
