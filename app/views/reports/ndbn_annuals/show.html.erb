<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "NDBN Annual Report - #{current_organization.name} - #{params[:year]}" %>
        <h1>
          <%= "#{params[:year]} NDBN Annual Report" %>
          <small>for <%= current_organization.name %></small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(dashboard_path) do %>
              <i class="fa fa-dashboard"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><%= link_to "All Reports", reports_ndbn_annuals_path %></li>
          <li class="breadcrumb-item"><a href="#">Reports - <%= params[:year] %></a></li>
        </ol>
      </div>
    </div>

    <div class="row mb-2">
      <%= render partial: 'partner_agencies_service_area', locals: { year: params[:year] } %>
      <%= render partial: 'summary_information', locals: { year: params[:year] } %>
    </div>
  </div>
</section>
