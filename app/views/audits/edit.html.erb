<% content_for :title, "Edit Audit - Inventory - #{current_organization.name}" %>

<div class="px-4 py-6 sm:px-6 lg:px-8">
  <%= render "shared/essentials/page_header",
        title: "Resume audit",
        subtitle: "Started #{@audit.created_at.strftime("%B %e, %Y").squeeze(" ")}.",
        back: {path: audits_path, label: "Back to audits"} %>
  <%= render partial: "audits/form" %>
</div>
