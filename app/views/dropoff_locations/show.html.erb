<section class="content-header">
<% content_for :title, "#{current_organization.name} - Agencies - Donation Sites - #{@dropoff_location.name}" %>
<h1>
  Donation Site Information
  <small>for <%= @dropoff_location.name %></small>
</h1>
<ol class="breadcrumb">
  <li><%= link_to(dashboard_path) do %>
    <i class="fa fa-dashboard"></i> Home
  <% end %>
  </li>
  <li><%= link_to "Donation Sites", (dropoff_locations_path) %></li>  
  <li class="active"><%= @dropoff_location.name %></li>
</ol>
</section>

<!-- Main content -->
<section class="content">

<!-- Default box -->
<div class="box">
  <div class="box-header with-border">
    <h3 class="box-title"><%= @dropoff_location.name %></h3>
  </div>
  <div class="box-body">

  <p><%= @dropoff_location.address %></p>
  
  <% if @dropoff_location.donations.present? %>
  <h4>Past Donations Received at this Location</h3>
  <div class="row">
  <div class="col-xs-12">
      <!-- /.box-header -->
      <div class="box-body table-responsive no-padding">
        <table class="table table-hover">
        <tr>
	      <th>Storage Location</th>
	      <th>Quantity of Items</th>
	      <th>Variety of Items</th>
	      <th>Source</th>
        </tr>

        <% @dropoff_location.donations.each do |donation| %>  
            <tr>
					  <td><%= donation.storage_location&.name %></td>
  					<td><%= donation.line_items.total %></td>
  					<td><%= donation.line_items.size %></td>
  					<td><%= link_to "View donation details", donation %></td>
          </tr>
          <% end %>        
        </table>
      <!-- /.box-body -->

  </div>
    <!-- /.box -->
  </div>
  </div>
  <% end %>

  </div>
</div>



