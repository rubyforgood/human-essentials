<section class="content-header">
<% content_for :title, "#{current_organization.name} - Agencies - Donation Sites - #{@dropoff_location.name} - Edit" %>
<h1>
  Editing Donation Sites
  <small>for <%= current_organization.name %></small>
</h1>
<ol class="breadcrumb">
<li><%= link_to(dashboard_path) do %>
    <i class="fa fa-dashboard"></i> Home
  <% end %>
  </li>
  <li><%= link_to "Donation Sites", (dropoff_locations_path) %></li>  
  <li class="active">Editing <%= @dropoff_location.name %></li>
</ol>
</section>

<!-- Main content -->
<section class="content">

<!-- Default box -->
<div class="box">
  <div class="box-header with-border">
    <h3 class="box-title">Update record for <%= @dropoff_location.name %></h3>
  </div>
  <div class="box-body">
  
		<%= render partial: "form", object: @dropoff_location %>
  </div>
</div>
<!-- /.box -->

</section>


