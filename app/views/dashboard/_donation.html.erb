<div class="row">
  <div class="large-8 columns">
    <%= link_to donation do %>
      <%= donation.total_quantity %> items from <%= donation.source %>
    <% end %>
  </div>
  <div class="large-4 columns">
    <%= distance_of_time_in_words_to_now donation.created_at %> ago
  </div>
</div>
