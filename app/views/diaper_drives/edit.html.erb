<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <% content_for :title, "Editing - Diaper Drive - #{current_organization.name}" %>
        <h1>
          Editing Diaper Drive
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><%= link_to(dashboard_path) do %>
              <i class="fa fa-dashboard"></i> Home
            <% end %>
          </li>
          <li class="breadcrumb-item"><%= link_to "Diaper Drives", (diaper_drives_path) %></li>
          <li class="breadcrumb-item"><a href="#">Editing <%= @diaper_drive.name %> on <%= @diaper_drive.created_at.to_s(:distribution_date) %></a></li>
        </ol>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</section>

<%= render 'form', diaper_drive: @diaper_drive %>
