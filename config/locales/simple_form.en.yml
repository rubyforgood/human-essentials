en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # `html` is set so the marker carries a class of its own. Styling it by attribute selector
      # would mean matching on the localised title -- "required" here, "necesario" in es -- and a
      # marker that only turns red in one locale is the kind of thing nobody notices.
      # `text` and `mark` are unused while `html` is set; both are left as documentation.
      html: '<abbr class="required-marker" title="required">*</abbr>'
    error_notification:
      # Nothing calls `error_notification` now -- the app renders `essentials_error_summary`. The
      # wording is kept correct anyway so the key is not a landmine: no "please", and no
      # "below", which is a position and fails WCAG 1.3.3 for anyone who cannot see the layout.
      default_message: "There is a problem with this form."
    # Examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'
